import {Alert, Button, Chip, Dialog, DialogActions, DialogContent, DialogTitle, Table, TableBody, TableCell, TableHead, TableRow} from '@mui/material'
const base=p=>p?.replace(/^.*[\\/]/,'')
export default function PreviewDialog({ops,onClose,onApply}) { const blocked=ops.some(x=>x.status.startsWith('conflict')); return <Dialog open={ops.length>0} onClose={onClose} fullWidth maxWidth="md"><DialogTitle>Rename preview</DialogTitle><DialogContent>{blocked&&<Alert severity="warning">Resolve conflicts before applying. Conflicting rows will be skipped.</Alert>}<Table size="small"><TableHead><TableRow><TableCell>Current</TableCell><TableCell>New</TableCell><TableCell>Status</TableCell></TableRow></TableHead><TableBody>{ops.map((op,i)=><TableRow key={i}><TableCell>{base(op.from)}</TableCell><TableCell>{base(op.to)}</TableCell><TableCell><Chip size="small" color={op.status==='ok'?'success':op.status==='unchanged'?'default':'error'} label={op.status}/></TableCell></TableRow>)}</TableBody></Table></DialogContent><DialogActions><Button onClick={onClose}>Cancel</Button><Button variant="contained" onClick={onApply} disabled={!ops.some(x=>x.status==='ok')}>Apply valid renames</Button></DialogActions></Dialog> }
